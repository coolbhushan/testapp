# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
#GET     /post                     controllers.Application.addTask()
POST     /addTodo						controllers.Application.addTodo()
GET     /deleteTodo                     controllers.Application.deleteTodo()
#GET    /edit                           controllers.Application.edit(id: Long)
POST  /saveTodo                       controllers.Application.saveEdit()
GET  /markTodo                       controllers.Application.markTodo()
#GET /data                            controllers.Application.getData()
#POST       /addTodo                       controllers.Application.addTodo()
#GET        /deleteTodo                    controllers.Application.deleteTodo()

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET     /assets/*file               controllers.Assets.versioned(path="/public", file)

